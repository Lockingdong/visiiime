const linkType = {
  normal: 'NORMAL',
  main: 'MAIN',
  media: 'MEDIA',
  collector: 'COLLECT'
};

const mediaOpenType = {
  ext: 'EXT', // 外部連結
  inr: 'INR' // 內部開啟
}

const linkArea = {
  main: 'main',
  normal: 'normal'
}

export {
  linkType,
  mediaOpenType,
  linkArea
}
